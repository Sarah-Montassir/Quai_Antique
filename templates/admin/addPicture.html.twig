{% extends base.html.twig %}

{% block title %}Carousselle Admin{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.picture) }}
    {{ form_row(form.isDisplayed) }}
    {{ form_end(form) }}
    <button class="btn btn-lg btn-primary" type="submit">Ajouter image</button>
{% endblock %}